const IP = 'prod.wanted-a.online'

export const EMAIL_API = `https://${IP}/users/:email`;
export const LOGIN_API = `https://${IP}/users/login`;
export const SIGNUP_API = `https://${IP}/users`;
export const PASS_RESET_API = `https://${IP}/users/:id/passwords`;